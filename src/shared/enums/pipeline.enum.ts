export enum StageActivityStatus {
  PENDING = "pending",
  IN_PROGRESS = "in_progress",
  DONE = "done",
  FAILED = "failed"
}

export enum FieldType {
  TEXT = "text",
  TEXTAREA = "textarea",
  EMAIL = "email",
  PHONE = "phone",
  NUMBER = "number",
  DATE = "date",
  SELECT = "select",
  MULTI_SELECT = "multi_select",
  CHECKBOX = "checkbox",
  RADIO = "radio",
  FILE = "file",
  URL = "url"
}

export enum TokenType {
  LOGIN = "login"
}
